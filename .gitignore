# Python venv
.venv/
__pycache__/
*.pyc

# Credentials — never commit (thetadata creds, db secrets)
# Note: db_secrets.py is committed (generic loader, no secrets); only db_secrets.json is ignored.
creds.txt
db_secrets.json
