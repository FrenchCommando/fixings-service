# Python venv
.venv/
__pycache__/
*.pyc

# Secrets — generated locally / on the Pi, never committed.
# The db_secrets.py / data_source.py loaders are committed (they read env, hold no secrets).
secrets/
.env
